import React, {useState, useEffect} from 'react'

function Home(props) {
    
    // const [items, setItems] = useState([]);

    // const url = `http://asia-south1-adon-interviews.cloudfunctions.net/getProductByCategory?category=${urlData}%27s-fashion`;

    //  const fetchData = async () => {
    //    const response = await fetch(url);
    //    const data = await response.json();
    //    const newData = data.data;
    //    setItems((prev) => [...prev, ...newData]);
    //  };
    //  useEffect(() => {
    //    fetchData();
    //  }, []);
    console.log(props)
    return (
        <div>
            <h1>hello</h1>
        </div>
    )
}

export default Home
